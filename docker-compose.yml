version: '3.1'

services:

  db:
    image: postgres:latest
    ports:
      - '5432:5432'
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
     - POSTGRES_DB=app_db
     - POSTGRES_USER=user1
     - POSTGRES_PASSWORD=12345
